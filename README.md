# Polymorphism-Assignment-Submission
This project demonstrates how to use interfaces, inheritance, and polymorphism in C#. An interface named IQuittable is created with a Quit() method. The Employee class implements this interface and provides its own version of the method. Polymorphism is shown by creating an object of type IQuittable and calling the Quit() method on it.
